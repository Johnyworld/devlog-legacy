import * as React from 'react';
import styled from 'styled-components';

const Container = styled.div``;

const Work: React.FC = () => {
    return (
        <Container>
            <p>Work</p>
        </Container>
    );
};

export default Work;

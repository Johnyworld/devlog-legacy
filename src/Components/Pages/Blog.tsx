import * as React from 'react';
import styled from 'styled-components';

const Container = styled.div``;

const Blog: React.FC = () => {
    return (
        <Container>
            <p>Blog</p>
        </Container>
    );
};

export default Blog;

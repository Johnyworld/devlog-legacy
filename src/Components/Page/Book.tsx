import * as React from 'react';
import styled from 'styled-components';

const Container = styled.div``;

const Book: React.FC = () => {
    return (
        <Container>
            <p>Book</p>
        </Container>
    );
};

export default Book;
